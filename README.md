# L2 Day 9 — Persona Execution and Inferencing

The agent requests structured JSON, validates it, clamps confidence, applies a threshold, and refuses uncertain execution.

Run `structured_decision.py`, followed by `02_guarded_execution.py`.

## Lab

Test ten utterances, build a confusion table, adjust the threshold, add a `summarize` intent, and require human approval for low-confidence calculations.
