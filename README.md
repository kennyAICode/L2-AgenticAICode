# L2 Day 15 — DevOps Foundations

Externalize configuration, produce JSON logs, add correlation IDs, measure latency and check Ollama health.

Run `python 01_structured_logging.py` and `python 02_health_check.py`.

Lab: log failures, never log prompts containing private data, add a model-readiness check and compare liveness with readiness.

