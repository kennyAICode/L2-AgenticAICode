# L2 Day 14 — HITL Capstone and Audit Trail

Create an approval queue, immutable identifiers, timestamps, reviewer decisions and a policy gate for risky actions.

Run `01_approval_queue.py` and `02_policy_gate.py`.

Lab: prevent double decisions, append audit events instead of overwriting, add expiry and separate proposer from approver.

