# L2 Day 11 — Document Ingestion and Chunking

Load text documents, split them with overlap, retain source metadata and save a JSON index.

```powershell
python 01_load_and_chunk.py
python 02_save_index.py
```

Lab: compare chunk sizes 8, 12 and 20; explain overlap; reject unsupported file types; add a document ID.

