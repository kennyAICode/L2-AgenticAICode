# L2 Day 12 — Vector Retrieval Fundamentals

Build TF-IDF vectors and cosine similarity with the Python standard library. This makes embedding-style retrieval mathematics visible before using a vector database.

Run `python 01_search.py` and `python -m unittest 02_test_store.py`.

Lab: add documents, change `top_k`, inspect zero-score matches and add a minimum-score threshold.

